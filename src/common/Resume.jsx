import React, { useEffect} from 'react';

const Resume = () => {
    useEffect(() => {
        window.location.replace('https://docdro.id/qNJVXMq');
    }, [])

    return (
        <div className="h-screen">
        </div>
    )
}

export default Resume;